import { CategoryCheckPipe } from './category-check.pipe';

describe('CategoryCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
